# Google Maps Distance Matrix API (optional)
# Add your key here to enable automatic distance calculation in the price calculator.
# Without it, the calculator works with manual distance input.
VITE_GOOGLE_MAPS_KEY=
